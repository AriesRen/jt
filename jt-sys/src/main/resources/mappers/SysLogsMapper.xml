<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.renhj.dao.SysLogsDao">

    <sql id="whereLikeUsername">
        <where>
            <if test="username != null and '' != username">
                username like concat("%", #{username}, "%")
            </if>
        </where>
    </sql>

    <select id="findLogById" resultType="SysLogs">
        select * from sys_logs where id=#{id}
    </select>

    <insert id="insertLog">
        insert into sys_logs(username, operation, method, params, time, ip, createdTime)
        values (#{username}, #{operation},#{method},#{params},#{time},#{ip},#{createdTime})
    </insert>

    <select id="findAllLogs" resultType="SysLogs">
        select * from sys_logs
    </select>

    <select id="findPageLogsByUsername" resultType="sysLogs">
        select * from sys_logs
        <include refid="whereLikeUsername"/>
        order by id desc
        limit #{index}, #{pageSize}
    </select>

    <select id="getRowCount" resultType="int">
        select count(*) from sys_logs <include refid="whereLikeUsername"/>
    </select>


</mapper>